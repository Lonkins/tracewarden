## What

<!-- What does this change and why -->

## Checklist

- [ ] `uv run pytest` passes; coverage holds
- [ ] `mypy --strict` and `ruff` clean
- [ ] New/changed detectors have synthetic fixtures (positive **and** negative cases)
- [ ] No real secrets or PII anywhere (fixtures are fabricated)
- [ ] Docs updated if behavior or public API changed
