"""Synthetic detector fixtures.

Everything here is fabricated: keys match a vendor's *format* so the regex fires,
but the bytes are random/structurally invalid and never authenticated any real
account. No real secret or real person's PII appears in this repo.
"""
